/--
Answer placeholder.

This should be a closed form expression (function mapping n to the answer in terms of n).

Definitions like the supremum of `LiuCanGuarantee` are not allowed - even though they would technically work,
just like saying "the answer is the maximum Liu can guarantee" is technically true but would yield 0 points at the IMO.
-/
def liu_bang_xiang_yu_solution : ℕ → ℝ := by
  sorry
